meta {
  name: DB 초기화
  type: http
  seq: 1
}

post {
  url: {{url}}/db/init
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  initData: @file(D:\coding\Project\final-02-twogether\api\dbinit\team\data.json)
  attach: @file()
}

settings {
  encodeUrl: true
}
