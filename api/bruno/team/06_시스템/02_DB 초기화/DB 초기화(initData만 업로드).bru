meta {
  name: DB 초기화(initData만 업로드)
  type: http
  seq: 2
}

post {
  url: {{url}}/db/init
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  initData: @file(D:\coding\Project\final-02-twogether\api\dbinit\team\data.json)
  ~attach: @file()
}

settings {
  encodeUrl: true
}
